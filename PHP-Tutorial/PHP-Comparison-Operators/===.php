<?php
    $a = 10;
    $b = "10";
    var_dump($a === $b);  // Returns true if $a is equal to $b and they are of same types.

    # Output :
        /*bool(false) */

    # Operator == :
        /* Identical */
?>
<?php
    $a = 10;
    $b = 20;
    var_dump($a >= $b);  // Returns true if $a is greater than or equal to $b.

    # Output :
        /*bool(false) */

    # Operator >= :
        /* Greater than than Equal to */
?>
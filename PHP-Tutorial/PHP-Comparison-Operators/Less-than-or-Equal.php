<?php
    $a = 100;
    $b = 20;
    var_dump($a <= $b);  // Returns true if $a is less than or equal to $b.

    # Output :
        /*bool(false) */

    # Operator <= :
        /* Less than or equal to */
?>
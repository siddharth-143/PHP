<?php
    $a = array(1, 2, 3);
    foreach($a as $value)
    {
        echo "value is $value <br>";
    }

    # Array :
        /* An array stores one or more similar type of values in a single value
        
        An array is a special variable, which can hold more than one values at a time.

        An array store multiple values in one single variable, you can access the values
        by referring to an index number.

        array :

            An array with a numberic key

            values are stored and accessed in linear fashion

            These arrays canstore numberics, strings and object but their index will be 
            represented by numbers

            By default array index starts from zero 

            The following ex shows two ways of creating an indexed array, the easiest way is */
?>
<?php
    $a = array(1,2,3,4,5);

    foreach($a as $b)
    {
        echo "value is $b <br>";
    }

    # foreach loop statement :
        /* The foreach statement  is used to ;oop through array.

        foreach loops through a block of code for each element in an array.

        For each pass the value of the current array element is assigned to $b
        and the array pointer is moved by one and in the next pass next element will be 
        processed.
        
        Output :
            value is 1
            value is 2
            value is 3
            value is 4
            value is 5 */
?>
<?php
    $str = "Hello World";
    echo strpos($str, "World");

    # Search for a specific text within a string / strpos() :
        /* The PHP strpos() function search for a specific text within a  string.
        
        If a match is found, the function returns the character position of the
        first match.

        If no match is found, it will return FALSE.
        */
?>
<?php
  
  $a = 10;
  $b = 20;
  $c = $a + $b;
  echo "Sum : ".$c;

  /* In php we declare variable using $ sign and add two variables values and
  store result in third variable.

  Two numbers can be added by passing input value in the form but without
  using (+) operator */
?>
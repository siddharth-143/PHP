<?php
    if (function_exists('in_array'))
    {
        echo "in_array() function is available";
    }
    else
    {
        echo "in_array() function is not availabel";
    }

    # function_exists():
        /* The function_exists is an inbuilt function in PHP.
        The function_exists() function is useful in case if we want to check whether a
        function() exists or not in the PHP script.
        It is used to check for both build-in functions as well as user-defined functions.
        */
?>
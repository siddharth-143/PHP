<?php
    $a = 1.984;
    $b = 1.2e9;
    echo $a;
    echo "<br>";
    echo $b;

    # Float :
        /* A float (floating point number) is a number with a decimal point or a number in
        exponential form.
        In the  following example $a is a float.
        Floating point numbers ( also known as "floats", "doubles", or "real number")
        can be specified.
        The float values of the given variable. Empty array return 0, non-empty 
        arrays returns 1. */
?>
<?php
    $a = 0;

    if(empty($a)) {
        echo $a." is empty or 0"."<br>";
    }

    if (isset($a)) {
        echo "Variable 'a' is set <br>";
        print_r($a);
    }

    # empty() :
        /* The empty() function is used to check whether a scenario, when you need to code in 
        fashion if a variable iss empty or another thigs if it is not so. 

        In these situations, empty function comes to rescue. */
?>
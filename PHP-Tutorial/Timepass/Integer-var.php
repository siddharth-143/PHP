<?php

$a = 100;
$b = 200;

echo $a;
echo "<br>";
echo $b;

/* In php, a variable start with the $ sign
You can change a variable's types by assiging a new values of a different types
A variable name cannot contain space
Variable used before they are assigned have default values.

Integer :
    Are whole numbers, without a decimal point, like 3427
    When you assign a interger values to a variable, no qutose around the value
    It could be either positive or negative if no sign precedes it is assumed to be positive.
    We assign an integer to the variable declared above and use the gettype function
    again to see the result. */
?>
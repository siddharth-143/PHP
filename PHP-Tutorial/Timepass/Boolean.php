<?php
    $a = 1;
    $b = 0;

    if($a)
        echo "a is true";

    if($b)
        echo "b is false";

    # Boolean :
        /* A boolean value is one that is in either of two states.
        They are known as True of False values, in programming.

        True is usally given a value of 1, and False is given a value of zero.

        $true_value = 1;
        $false_value = 0;

        You can replace the 1 and0 with the words "true" and "false" (without the quotes). */
?>